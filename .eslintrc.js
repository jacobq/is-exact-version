module.exports = {
    extends: "airbnb-base",
    parser: "typescript-eslint-parser",
    root: true,
    overrides: [{
        files: ["**/*"],
        rules: {
            // (might add custom rules here in the future)
        }
    }]
};